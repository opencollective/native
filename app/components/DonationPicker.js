import React, { PropTypes } from 'react-native';

const { View, Text } = React;

class DonationPicker extends React.Component {
  render () {
    return (
      <View>
        <Text>Donation Picker</Text>
      </View>
    )
  }
}

export default DonationPicker;
