'use strict';

import React from 'react-native'
const { View, Text } = React;

class AsyncButton extends React.Component {
  render () {
    return (
      <View>
        <Text>AsyncButton</Text>
      </View>
    )
  }
}

export default AsyncButton;
